#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char low, e, q;
e = 'e';
q = 'q';
for (low != e && low !=q)
{
if (low != e && low !=q
putchar(low);
}
printf("\n");
returm (0);
}
